/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: "",
    basePath: "/portfolio",
    images: {
        unoptimized: true,
    },
    output: "export"
};

export default nextConfig;
